{
  "name": "inngest-tools",
  "version": "0.0.0",
  "description": "Static analysis CLI for Inngest step functions",
  "bin": {
    "inngest-tools": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@inngest-tools/core": "workspace:*",
    "@inngest-tools/lint": "workspace:*",
    "@inngest-tools/viz": "workspace:*",
    "chalk": "^4.1.2",
    "commander": "^13.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "license": "MIT"
}
